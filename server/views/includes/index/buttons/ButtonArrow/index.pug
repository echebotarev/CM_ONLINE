extends ../../button
block style
    style
        include style.css
block internal
    div
        .top.border(style={'border-bottom-color': `${backgroundColor} !important`})
        .bottom.border(style={'border-top-color': `${backgroundColor} !important`})
        .body(style={'background': `${backgroundColor}`})
        include ../link
